import styled from 'styled-components'

export const StyledInfoCard = styled.div`
  .info-card--name {
    font-weight: 600;
    font-size: 1.49em;
    margin-top: 0.7em;
    margin-bottom: 0.7em;
  }
  .info-card--desc {
    font-weight: 300;
    font-size: 1.17em;
    line-height: 1.5em;
  }
`
